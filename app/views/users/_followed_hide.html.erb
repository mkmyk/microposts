<%= form_for(current_user, 
              url: followed_show_path, 
              html: { method: :get },
              remote: true) do |f| %>
  
  <div><%= hidden_field_tag :followed_show, true %></div>
  <%= f.submit "Who Follow Me", class: "btn btn-primary" %>

<% end %>