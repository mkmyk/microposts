Followed by (<%= current_user.follower_users.count %>) user(s)

<ul>
    <% current_user.follower_users. each do | follow | %>
  <li><%= follow.name %>（<%= follow.id %>）</li >
<% end %>
    </ul>
    
<%= form_for(current_user, 
              url: followed_hide_path, 
              html: { method: :get },
              remote: true) do |f| %>
  
  <div><%= hidden_field_tag :followed_show, false %></div>
  <%= f.submit "Hide", class: "btn" %>

<% end %>